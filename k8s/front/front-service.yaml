apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: sbuasa
spec:
  type: ClusterIP
  selector:
    app: streamlit
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
